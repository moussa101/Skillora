{
  "name": "ai-resume-analyzer",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "backend/{src,test}/**/*.ts": [
      "cd backend && npx eslint --fix"
    ],
    "frontend/src/**/*.{ts,tsx}": [
      "cd frontend && npx eslint --fix"
    ]
  }
}
